service: serverless-iidx-csv-parser

provider:
  name: aws
  runtime: go1.x
  region: ap-northeast-1
  profile: mizukmb
  stage: production
package:
 exclude:
   - ./**
 include:
   - ./bin/**
   - ./iidx24_sinobuz_score.csv
functions:
  parse:
    handler: bin/main
